// ParamDataType type of the value of the parameter. It must be validated
export enum ParamDataType {
    Boolean = 'boolean',
    Integer = 'integer',
    Float = 'float',
    Enum = 'enum',
    String = 'string',
    Password = 'password'
}
